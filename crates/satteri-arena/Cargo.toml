[package]
name = "satteri-arena"
version = "0.2.2"
edition = "2021"
description = "Arena allocator and binary buffer primitives for Sätteri"
license = "MIT"

[dependencies]
memchr = { workspace = true, default-features = true }
rustc-hash = { workspace = true }

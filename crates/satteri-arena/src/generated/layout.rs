//! @generated by `cargo run -p satteri-layout-codegen`. Do not edit by hand.
//!
//! Raw-buffer layout: header offsets for `Arena::to_raw_buffer` and asserts
//! pinning the schema-declared `ArenaNode` offsets (mirrored by the JS readers
//! via `packages/satteri/src/generated/arena-layout.ts`) to the real structs.

use crate::kind::{ArenaKind, Hast, Mdast};
use crate::node::ArenaNode;

/// Raw-buffer header byte offsets (4-byte fields, in write order).
pub(crate) mod header {
    pub(crate) const MAGIC: usize = 0;
    pub(crate) const KIND: usize = 4;
    pub(crate) const NODE_STRUCT_SIZE: usize = 8;
    pub(crate) const NODE_COUNT: usize = 12;
    pub(crate) const NODES_OFFSET: usize = 16;
    pub(crate) const CHILDREN_COUNT: usize = 20;
    pub(crate) const CHILDREN_OFFSET: usize = 24;
    pub(crate) const TYPE_DATA_LEN: usize = 28;
    pub(crate) const TYPE_DATA_OFFSET: usize = 32;
    pub(crate) const STRING_POOL_LEN: usize = 36;
    pub(crate) const STRING_POOL_OFFSET: usize = 40;
    pub(crate) const NODE_DATA_COUNT: usize = 44;
    pub(crate) const NODE_DATA_OFFSET: usize = 48;
    pub(crate) const SIZE: usize = 52;
}

const _: () = {
    assert!(core::mem::size_of::<ArenaNode>() == 52);
    assert!(core::mem::offset_of!(ArenaNode, id) == 0);
    assert!(core::mem::offset_of!(ArenaNode, node_type) == 4);
    assert!(core::mem::offset_of!(ArenaNode, parent) == 8);
    assert!(core::mem::offset_of!(ArenaNode, start_offset) == 12);
    assert!(core::mem::offset_of!(ArenaNode, end_offset) == 16);
    assert!(core::mem::offset_of!(ArenaNode, start_line) == 20);
    assert!(core::mem::offset_of!(ArenaNode, start_column) == 24);
    assert!(core::mem::offset_of!(ArenaNode, end_line) == 28);
    assert!(core::mem::offset_of!(ArenaNode, end_column) == 32);
    assert!(core::mem::offset_of!(ArenaNode, children_start) == 36);
    assert!(core::mem::offset_of!(ArenaNode, children_count) == 40);
    assert!(core::mem::offset_of!(ArenaNode, data_offset) == 44);
    assert!(core::mem::offset_of!(ArenaNode, data_len) == 48);
    assert!(u32::from_le_bytes(crate::raw_buffer::BUFFER_MAGIC) == 0x5241444d);
    assert!(<Mdast as ArenaKind>::KIND_TAG == 1);
    assert!(<Hast as ArenaKind>::KIND_TAG == 2);
};

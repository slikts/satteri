{
  "name": "@tryckeri/core",
  "version": "0.1.0",
  "description": "High-performance Markdown processing with Rust arena backend",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./arena-reader": "./src/arena-reader.ts"
  },
  "napi": {
    "binaryName": "tryckeri_napi",
    "targets": [
      "x86_64-unknown-linux-gnu",
      "x86_64-apple-darwin",
      "aarch64-apple-darwin",
      "x86_64-pc-windows-msvc",
      "wasm32-wasip1-threads"
    ]
  },
  "scripts": {
    "build:native": "napi build --manifest-path ../../crates/napi-binding/Cargo.toml --output-dir . --platform --release",
    "build:native:debug": "napi build --manifest-path ../../crates/napi-binding/Cargo.toml --output-dir . --platform",
    "build:ts": "tsc",
    "build": "pnpm build:native && pnpm build:ts",
    "dev": "tsc --watch",
    "test": "vitest run",
    "bench": "vitest bench --run"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "vitest": "^4"
  }
}
